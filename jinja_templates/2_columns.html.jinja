<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8" />
  <title>Ревью сотрудника {{ name }}</title>
  <style>
    :root {
      --page-width: 900px;
      --gap: 24px;
      --title-size: 32px;    /* h1 */
      --subtitle-size: 24px; /* секционные заголовки */
    }
    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Arial, "Noto Sans", "Helvetica Neue", sans-serif;
      line-height: 1.5;
      margin: 40px;
      color: #111;
    }
    .page {
      max-width: var(--page-width);
      margin: 0 auto;
    }

    /* Главный заголовок (по центру) */
    h1 {
      text-align: center;
      font-size: var(--title-size);
      margin: 0 0 24px 0;
      font-weight: 700;
    }

    /* Заголовки секций — больше, без тире */
    .section-title {
      font-weight: 700;
      font-size: var(--subtitle-size);
      margin: 0 0 10px 0;
    }

    /* Центровка именно для «Советы по улучшению» */
    .section-title--center {
      text-align: center;
    }

    .columns {
      display: flex;
      gap: var(--gap);
      justify-content: space-between;
      margin-bottom: 24px;
    }
    .column {
      flex: 1 1 0;
      min-width: 0;
    }

    /* Списки: без стандартных маркеров, длинное тире как маркер и текст по ширине */
    ul.list {
      list-style: none;
      padding: 0;
      margin: 0;
    }
    ul.list li {
      margin: 6px 0;
      text-align: justify;   /* выравнивание по ширине для текста пунктов */
    }
    ul.list li::before {
      content: "— ";         /* длинное тире перед пунктом */
      color: inherit;
    }

    .suggestions {
      margin-top: 8px;
    }
  </style>
</head>
<body>
  <div class="page">
    <!-- 1. Оглавление -->
    <h1>Ревью сотрудника {{ name }}</h1>

    <!-- 2. Сильные стороны / Слабые стороны (2 колонки) -->
    <div class="columns">
      <div class="column">
        <div class="section-title">Сильные стороны</div>
        <ul class="list">
          {% for item in strengths %}
            <li>{{ item }}{% if loop.last %}.{% else %};{% endif %}</li>
          {% else %}
            <li>нет записей.</li>
          {% endfor %}
        </ul>
      </div>
      <div class="column">
        <div class="section-title">Слабые стороны</div>
        <ul class="list">
          {% for item in weaknesses %}
            <li>{{ item }}{% if loop.last %}.{% else %};{% endif %}</li>
          {% else %}
            <li>нет записей.</li>
          {% endfor %}
        </ul>
      </div>
    </div>

    <!-- 3. Советы по улучшению (одна колонка; заголовок по центру) -->
    <div class="suggestions">
      <div class="section-title section-title--center">Советы по улучшению</div>
      <ul class="list">
        {% for tip in suggestions %}
          <li>{{ tip }}{% if loop.last %}.{% else %};{% endif %}</li>
        {% else %}
          <li>нет записей.</li>
        {% endfor %}
      </ul>
    </div>
  </div>
</body>
</html>